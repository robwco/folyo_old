= simple_form_for @survey, url: survey_path(@survey), as: :survey, wrapper: :survey do |f|

  %a.survey-intro-link{href: '/surveys/budget/introduction'} The 2014 Folyo Budget Survey

  = render '/surveys/partials/pagination', {current_page: 2, position: 'top'}

  - skill = BudgetSurveyData.skills[:logo_and_identity_design]

  .survey-intro
    :markdown
      Here are two sample identity design projects. How much would you have charged for each of them?

      Note: be sure to **view the full case study** for each project.

  = render layout: '/surveys/partials/form_block' do
    = render '/surveys/partials/project', project: skill[:project_types][:logo]
    = render '/surveys/partials/budget', input_name: :logo_budget, input_label: 'For a similar project, I would charge at least…', f:f

  = render layout: '/surveys/partials/form_block' do
    = render '/surveys/partials/project', project: skill[:project_types][:full_identity]
    = render '/surveys/partials/budget', input_name: :full_identity_budget, input_label: 'For a similar project, I would charge at least…', f:f

  = render '/surveys/partials/pagination', {current_page: 2, position: 'bottom'}
