- if artwork
  = image_tag artwork.asset.url(:small)
  = link_to 'Remove artwork', designer_project_artwork_path(project.designer, project, artwork), class: 'remove', method: 'DELETE', confirm: 'Are your sure you want to remove your artwork?'
